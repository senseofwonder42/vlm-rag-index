You are organising a corpus of documents to answer a search query. You must pick the single metadata axis whose values best separate documents relevant to the query from irrelevant ones.

Query:

{{ query }}

Available axes and the distinct values present in the current candidate set:

{% for axis, values in axes %}- `{{ axis }}`: {{ values | join(", ") }}
{% endfor %}
Pick exactly one axis from the list above. Choose the axis whose values most sharply discriminate documents that are relevant to the query. Return the axis name exactly as written.
